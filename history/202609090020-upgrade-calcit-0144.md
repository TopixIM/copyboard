# Upgrade to Calcit 0.14.4

Upgrade Calcit and `@calcit/procs` to 0.14.4 and refresh the released Calcit dependency graph. Use the official compatibility diagnostics while the legacy Dynamic contracts are migrated.

Fix the client WebSocket host and port query fallback, unwrap the server session returned by `get-in`, initialize preview state as a list, replace deprecated enum matching, and use the typed map filtering API. Work around the released `calcit.std` date constructor diagnostic at the application boundary.

The native smoke test now proves that `storage.cirru` was loaded and that its last snippet is returned over HTTP, in addition to checking the HTTP and WebSocket servers. Node 24 is pinned for Vite 8. Local browser verification uses `http://localhost:5173/?mode=dev&host=localhost&port=11006`.
