# Calcit 0.13.29 upgrade

- Migrated CI and project commands from `cr`/`setup-cr` to `calcit`/`setup-calcit`.
- Kept the canonical `calcit.cirru` snapshot, removed the legacy `compact.cirru`, and formatted the snapshot with Calcit 0.13.29.
- Refreshed dependency resolution and Yarn metadata for the latest available main/upgrade branches.
- Verified default and server entries with check-only mode, generated JavaScript, and ran the frontend build (the local Vite run requires Node 20.19+; CI uses Node 24).
